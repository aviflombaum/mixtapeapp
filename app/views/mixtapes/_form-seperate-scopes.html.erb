<%= form_for(@mixtape, :html => {:multipart => true}) do |f| %>
  <% if @mixtape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mixtape.errors.count, "error") %> prohibited this mixtape from being saved:</h2>

      <ul>
      <% @mixtape.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name%><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description, "Describe the mixtape"  %><br />
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= label_tag('song[name]', 'Song Name')%><br />
    <%= text_field_tag('song[name]')%>
  </div>

  <div class="field">
    <%= label_tag('song[audio]', 'Song Audio')%><br />
    <%= file_field_tag('song[audio]')%>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
